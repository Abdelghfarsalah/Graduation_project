import 'package:flutter/material.dart';

class Textandimage extends StatelessWidget {
  const Textandimage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("Textandimage")],
    );
  }
}
